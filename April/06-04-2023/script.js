const Quote = document.getElementById(Quote);
const QuoteBtn = document.getElementById(QuoteBtn);

QuoteBtn.addEventListener('click'.Generate)

Generate()

function Generate()
{
    document.getElementById('Quote').innerHTML = "Good luck"
}
